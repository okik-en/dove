#import "../typ/templates/report.typ": *
#import "../typ/utils/replace.typ": *
#show: simple-report
#show: replace

#set page("a4")

#set list(indent: 2em, spacing: 1em)
#set terms(indent: 2em, spacing: 1em)
#set enum(indent: 2em, numbering: it => context numbering("A-1:", counter(heading).get().first(), it), spacing: 1em)
#set grid(gutter: 2em, align: top)
#show strong: set text(black)
#show math.equation: it => math.display(it)

#let ans = box(text("解答", red, weight: "black", font: family.sans), stroke: 1pt + red, inset: 4pt, radius: 4pt)

#set heading(numbering: "[A]")

#set document(
  title: "2026 データベース 春学期中間試験",
  author: "Salty Lemon " + emoji.lemon,
  date: datetime.today(),
)

#let ai = text(red, sym.dagger + "AIによる生成" + h(.5em))

#title()
#author()

以下、#ai;のついた解答はMicrosoft Copilotによるものである。

=

+ 実世界とデータベースの関係を、図を用いて説明しなさい。
  特に以下を必ず含めること： 記号系、データモデル、データモデリング
+ 上記の図で、「実世界 #sym.arrow データベース」への変換において、どのような情報が失われるか（または抽象化されるか）。

#ans

+ 実世界とデータベースの関係を#[@fig:rel]に示す。
  実世界で生気している様々な事象を記述するためには、何らかの#underline[記号系]が必要である。
  これを#underline[データモデル]という。
  #underline[データモデル]に基づいて実世界をデータベース化する過程を#underline[データモデリング]という。
  #figure(
    caption: [実世界とデータベースの関係 #h(2em) (教科書の図より引用)],
    image("./db-1-1.jpg"),
  ) <fig:rel>
+ #ai;現実世界の連続的で多様な事象は、データベースでは属性や表に還元されるため、文脈・曖昧さ・感情・例外的要素などの細かな情報が捨象される。
  また、必要な項目のみを選ぶ過程で主観的な取捨選択が入り、完全な再現はできない。

#pagebreak()

=

+ データの独立性とは、何を何から独立させることか説明しなさい。
+ その考えが導入された動機や背景を説明しなさい。
+ データ独立性が存在しない場合、システムにどのような問題が発生するか。
  できるだけ具体的なシステム例を用いて説明しなさい。

#ans

+ データの独立性とは、アプリケーションプログラムをデータベースの諸元から切り離すことをいう。
+ 1960年代、情報システムが巨大化していく中で、ソフトウェアの生産性をどのようにして上げていくかという難題、ソフトウェア危機が生じた。
  コッドは、ソフトウェア危機を乗り越えるには、データの独立性を確保し、アプリケーションプログラムをそれらの改変と無関係にすることを考えた。
+ #ai;データ独立性がない場合、データ構造の変更がアプリケーションへ直接影響する。
  例えばECサイトで顧客テーブルを分割・列追加すると、購入処理や検索機能など関連する全プログラムの修正が必要となり、バグ発生や開発遅延を招く。
  結果として保守性が低下し、運用コストも増大する。

#pagebreak()

=

+ 閉世界仮説は本来どの分野の概念か。また、その意味を説明しなさい。
+ データベースにおける閉世界仮説とは何か、具体例を用いて説明しなさい。
+ 閉世界仮説が成立しない場合を1つ挙げ、そのときにどのような問題が生じるか説明しなさい。
+ 次の$2$つを比較せよ：
  #align(center)[閉世界仮説（Closed World Assumption）#h(1em)開世界仮説（Open World Assumption）]
  さらに、データベースにおいて、それぞれが適している場面を説明しなさい。

#ans

+ 閉世界仮説は論理学の概念で"現時点で真であると判明していないことは偽とする"という推定を表す。
+ データベースはこの仮説に立脚していて、"現時点でデータベースに記録されていない事象は実世界で生気していない"とする。
  したがって、リレーション友人を例にとれば、現時点でリレーション友人に記録されていなければ友人でないということである。
+ #ai;閉世界仮説が成り立たない例として、Web上の知識ベースがある。
  そこでは未登録の事実が単に未入力なだけであり、偽とは限らない。
  この状況で閉世界仮説を適用すると「データベースにない＝偽」と誤判断し、誤った結論や検索結果を導く問題が生じる。
  結果として、情報の不完全性が誤推論や信頼性低下を招く。
+ #ai;閉世界仮説（CWA）は「データベースに存在しない事実は偽」とみなす考え方であり、完全に情報が管理されている業務データに適する。
  一方、開世界仮説（OWA）は「未知は不明であり偽とは限らない」とする立場で、Web知識や不完全なデータを扱う場面に有効である。
  例えば企業の在庫管理ではCWAにより効率的な判定が可能だが、学術知識ベースではOWAを用いないと誤った否定判断を招く。


#pagebreak()

=

次の関係がある：
#align(center)[得点(学生番号, 科目, 点数)]

この表に対して、以下の問いに答えなさい。
+ 学生ごとに全科目の点数の平均を求め、平均が$80$点以上の学生の学生番号と平均点を求めるSQL文を記述しなさい。
+ 上記のSQLについて、`GROUP BY`は何を意味しているか、なぜ`WHERE`ではなく`HAVING`を用いるのか、説明しなさい。
  単なる用語の説明ではなく、本問に即して説明すること。
+ 上記のSQLにおいて、もし`HAVING`の代わりに`WHERE`を用いた場合、どのような問題が生じるか なぜその問題が起きるのかを説明しなさい。

#ans

+ #[@sql]にSQL文を示す。
  #figure(
    caption: [SQL文],
    ```sql
    SELECT 学生番号, AVG(点数)
    FROM 得点
    GROUP BY 学生番号
    HAVING AVG(点数) >= 80;
    ```,
  ) <sql>
+ #ai;本問では学生ごとに平均点を求める必要があるため、`GROUP BY` 学生番号により同一学生の複数科目の行を1つのまとまりとして集約する。
  これにより各学生について`AVG(点数)`が計算できる。
  次に、求めた平均点に対して「80点以上」という条件を課すが、この条件は集約後の結果に対するものである。
  そのため、集約処理の後に条件を適用できる`HAVING`を用いる。
  つまり、`GROUP BY`は「学生単位への集約」、`HAVING`は「その集約結果に対する条件指定」を意味する。
+ #ai;`WHERE`は集約処理より前に各行へ条件を適用するため、平均値のような集約結果を直接条件に使うことができない。
  仮に`WHERE 点数 >= 80`のように書くと、80未満の科目の点数が事前に除外され、その後で平均が計算される。
  その結果、本来は平均80未満の学生でも、高得点の科目だけで平均が算出され、80以上と誤判定される可能性がある。
  このように、集約前に条件を適用してしまうことで、計算対象が変わり、意図と異なる誤った結果が生じる。
